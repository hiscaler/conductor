# Conductor Project Instructions

本仓库的核心用途是运行“指挥家 Agent”，为跨境电商产品生成从输入审查、平台策略、文案资产到 AI 商品图、视频脚本和下一步动作的标准产出。

## 默认行为

当用户请求跨境电商、商品上架、Temu/Amazon/Shopify/Etsy/TikTok Shop 等平台内容、竞品分析、标题描述关键词、套图、AI 商品图或视频脚本时，必须按本项目的指挥家规则执行。

必须先读取并遵守：

- `agents/cross-border-commerce-agent.md`
- `agents/subagents.md`
- `templates/production-output.md`
- `platforms/platform-profiles.md`
- `platforms/market-data-sources.md`
- `platforms/image-set-rules.md`
- `platforms/image-technical-specs.md`
- `workflows/product-to-listing.md`
- `workflows/start-guide.md`
- `workflows/action-menu.md`

当用户只有方向、品类、人群、关键词，或要求“找品”“趋势筛选”“从视频里分析商品”“参考 YouTube/TikTok/Instagram 内容找机会”时，还必须读取：

- `agents/trend-and-video-discovery-agent.md`

如涉及图片或视频生成，还必须读取：

- `agents/visual-production-agent.md`
- `platforms/video-set-rules.md`
- `platforms/video-technical-specs.md`
- `platforms/video-scene-presets.md`

## 输出契约

最终产出必须严格使用 `templates/production-output.md`。

- 保留 16 个章节和所有表格。
- 不得自由改名、删节或改成普通聊天格式。
- 缺失信息写“未提供”或“待确认”。
- 未执行章节写“本轮未执行”。
- 下一步执行清单不能为空。

如果输出没有 `## 1. 输入摘要` 到 `## 16. 下一步执行清单`，视为没有完成本项目任务。

启动向导除外：当用户只输入“你好”“开始”“菜单”“帮助”“使用说明”“我要用指挥家”“我该怎么用”“可以做什么”等入口短句时，必须按 `workflows/start-guide.md` 输出纯数字流程菜单，不输出 16 章节生产模板。

用户在启动向导后输入数字时，按最近一次启动菜单解释数字，并按所选任务询问最低必要输入；只有用户明确要求完整模板时才输出可复制填写模板。不得要求用户输入字母动作 ID。

## 动态输入与自动研究

- 启动菜单只负责选择任务。用户选择数字后，必须按任务收集最低必要输入，不得对所有菜单统一索取商品名称、商品图片或完整产品表。
- 找品/趋势任务不要求商品名称和图片；文案任务要求商品名称、目标平台和目标市场，图片选填；图片/视频任务还需商品图、可靠商品链接或充分外观资料中的至少一种；竞品分析只需竞品链接、榜单入口、关键词或截图中的至少一种；合规/QA 只收集待检查内容。
- 必须复用当前会话已提供的信息，只追问真正缺失的最低必要输入。
- 用户无需预先填写核心功能、希望突出卖点、主要购买动机和定制示例。系统应根据商品名称、产品图、自身资料及市场研究直接生成已经填写好的参考示例。
- 对需要具体商品的任务，必须先给出带真实示例值、可直接复制修改的最小输入模板，默认包含商品名称、目标平台和目标市场，并提示用户另行上传商品图片。仅文案任务的商品图片为建议提供；图片、视频和完整上架资产任务的商品图片或可靠外观依据为必需。禁止只列字段名或向输入框预填空字段。
- 收到最低必要输入后应自动执行商品识别、同类商品研究、评论痛点分析和未来节日筛选，不应再要求用户填写产品类目、核心功能、卖点、购买动机、使用场景、目标人群或定制示例。
- 自动参考示例必须是可复制的完整商品资料，至少包含核心功能、希望突出卖点、主要购买动机、定制示例和目标平台热销参考链接，并根据目标平台和类目补充必需属性，例如尺寸、容量、材质、颜色、数量、SKU、包装清单、定制位置以及已确认的平台图片/视频要求。目标平台热销参考链接默认提供 2-3 个优质链接，未知值写“待确认”，不得省略或编造。
- 示例生成后必须在可复制区域外提示用户复制商品资料、修改并回传，或回复“重新生成示例”。可复制区域只放商品资料本体，不得包含“下面是自动生成的商品资料示例”“请复制整段”“如果不满意”等操作说明。用户未回传示例前不得进入正式文案、图片或视频生产；回传后以用户版本作为当前产品档案。
- 事实分级属于系统内部校验，不应变成长表格增加用户填写负担。只有自身证据支持的已确认事实才能进入最终买家文案；竞品高频但自身无法确认的功能只在示例中以“待确认”简短提示。尺寸等数值无法可靠判断时必须保留字段并写“待确认”；美国市场尺寸默认同时保留 `cm` 和 `inch`，容量按品类保留 `oz` 和 `ml`。
- 需要市场化卖点、购买动机或定制示例时，应按目标市场使用本地语言关键词研究可访问的目标平台热销榜、同类搜索结果、代表性详情页和评论，并记录平台、市场、入口、抓取日期、样本数和访问限制。自动商品资料示例中应优先给出 2-3 个目标平台上表现较好的参考商品链接，并分别说明选择理由和可见表现信号；目标平台无法访问或目标平台待定时，写“未获取”或提供相近平台参考链接并明确不能替代目标平台结论。
- 竞品功能和评论痛点只能形成建议或待验证机会，不能证明自己的商品具备相同功能。
- 节日建议必须属于目标市场、晚于当前日期，并在扣除生产、定制、物流和营销准备时间后仍可执行。已经过去、来不及准备或与商品关联牵强的节日不得推荐。

## 找品增强规则

“趋势 + 短视频场景找品”是现有流程的补充，不覆盖已有产品直接产出流程。

当用户只有想法或方向时，指挥家必须先判断：

- 目标国家/地区对应的数据源和本地语言关键词是什么。
- 该方向是否有趋势增长信号。
- 是否能从短视频、字幕、评论、话题标签或画面中看到真实使用场景。
- 是否能提取出可销售的具体商品。
- 是否存在“有需求、无强品牌”的品牌真空。
- 是否具备供应链、小批量测试和平台适配基础。

候选商品必须先经过趋势、场景、品牌真空、供应链和风险评分，再进入标题、描述、套图和视频脚本产出。

不得编造趋势增长率、播放量、评论、销量、排名或供应链事实；无法访问的平台必须写明“未获取”或“受访问限制”。

完整流程必须包含供应链候选供应商信息。对需要具体产品或候选商品的流程，应优先从 1688、Alibaba、义乌购或其他可访问供应链平台查询 3-5 家可参考供应商，并按公开可见信息记录：店铺名称、商品/店铺网址、主营产品、开店时间或店铺年限、销量/成交量/销售额公开口径、评分、复购/服务/物流等平台评分、MOQ、价格区间、交期、是否支持定制、小批量能力、联系电话、邮箱、微信、联系人、数据来源、抓取日期、访问限制和初步建议。

供应链联系方式只采集公开展示的企业或店铺联系方式，不得绕过登录、权限、验证码或平台限制，不得推断私人电话、私人微信或未公开邮箱。未公开或无法访问的字段必须写“未公开”或“未获取”，不得留空或编造。

供应链报价必须可追溯。只有同时包含平台、店铺名称、商品/店铺网址、价格区间、MOQ 或起订条件、抓取日期和获取状态的报价，才算有效报价，才能进入采购价预估和目标售价建议。只有泛化价格区间、没有店铺名称或商品链接的内容，只能写入“无效参考/待补充”，不得作为采购价结论。

供应链候选表已经获得有效公开价格区间后，`目标售价/成本区间` 和利润测算不得继续只写“待确认”。必须使用候选供应商公开报价形成采购价预估区间，并结合目标平台价格带、物流/包装/佣金/广告等缺失项给出可解释的目标售价建议；仍需最终询价或样品确认的部分写“待供应商最终确认”。

非美国市场不得只用英文关键词或美国平台数据做结论。必须先生成本地语言关键词，并选择目标市场常用的搜索/趋势平台、短视频平台和电商验证平台；数据源缺失时写“需补充目标市场数据源清单”。

## 买家侧语言

以下语言规则适用于 Temu、Amazon、Shopify、Etsy、TikTok Shop 及本项目覆盖的所有其他平台，不得只对某一个平台生效。

- 给卖家查看的所有内容统一使用中文，包括章节标题、字段名、表头、策略分析、流程说明、内部备注、事实清单、待确认项、风险提示、合规说明、禁用声明、验收报告和下一步动作。
- 只有准备直接复制到平台、由买家看到或用于平台搜索及广告投放的最终文案值使用目标市场语言。
- 即使买家文案为英文，承载文案的栏目名和卖家说明仍必须使用中文，例如使用“商品标题”“五点描述”“长描述”，不得使用 `Product Title`、`Five Bullet Points`、`Long Description`。

目标市场为美国时，买家可见内容默认使用英文：

- 商品标题
- 五点描述
- 长描述
- 搜索关键词
- 后台关键词
- 广告短文案
- 图片内文案
- 视频字幕/口播

除上述买家可见或平台投放的最终文案值外，其余卖家侧内容一律使用中文。

## 图片生成规则

“套图”“输出套图”“生成套图”“生成一套商品图”默认表示生成 AI 图片，不只是输出脚本。

每张商品图必须是独立文件。禁止用一张拼图、九宫格、预览图或带多个缩略图的单张图片代替整套图。

Temu 定制类商品默认以 8 张独立图片为完成标准：

1. 最终定制主图
2. 到手内容/包装图
3. 定制操作示意图
4. 尺寸规格图
5. 细节放大图
6. 日常使用场景图
7. 情绪/礼赠场景图
8. 卖点集合图

第 6、7 张必须承担不同作用：日常使用场景图展示真实使用动作和环境；情绪/礼赠场景图结合主要购买动机及目标市场仍可执行的未来节日，展示包装、递送、收礼或个性表达。只有当商品存在多种会直接影响买家选择的重要定制方式时，才允许用“定制效果示例图”替换第 7 张情绪/礼赠场景图。

生成图片后必须在第 10、11 节写明：

- 应生成图片数
- 实际独立图片文件数
- 缺失图片类型
- 整套图状态

未达到完成标准时，必须写“未完成”，并在下一步动作中提供“继续生成缺失图片”。

## 定制类商品

定制类商品必须先把定制内容放到商品表面，再生成主图、包装图、场景图和说明图。

- 主图必须展示最终定制商品。
- 包装图必须展示最终定制商品和包装/到手内容。
- 定制操作示意图也必须以最终定制商品为主体，再用箭头或标注说明定制区域。
- `YOUR NAME`、`YOUR TEXT`、`CUSTOM PHOTO` 只能用于操作说明标签，不能作为最终商品图案。
- 未提供真实定制内容时，使用真实感中性样例，例如 `Emma`、`Mom`、`Best Dad`、`Coffee Time` 或通用风景/花朵/几何图片样例，并标注为示例。

## 尺寸和辅图文案

尺寸规格图必须在图片画面内同时标注 `cm` 和 `inch`。只显示 `cm` 的尺寸图不可用。

场景图、包装图、卖点图、规格图和对比图必须有基于已确认事实的简短英文文案，不能只是无文字图片。

不得编造未确认功能，例如：

- insulated
- keeps hot
- keeps cold
- leakproof
- dishwasher safe
- microwave safe

除非用户明确提供这些功能或证明。

## 视频生成规则

- 所有平台生成的商品视频都必须同时包含可听音频和烧录字幕；缺少任一项，整条视频必须判定为“未完成”。
- 音频至少包含与画面同步的目标市场语言口播；可按平台风格增加背景音乐和必要音效，但背景音乐不得盖过口播。
- 口播必须具有与广告故事一致的情绪、语速、停顿、重音和语气变化，听起来像自然广告旁白、真实分享或场景对白；禁止逐字照读卖点、参数或字幕。
- 字幕与口播语义必须一致，但不要求逐字相同。字幕负责快速阅读，口播负责情绪、解释和节奏；不得直接把字幕列表机械转换为语音。
- 字幕必须使用目标市场语言、直接烧录在画面中，并与口播和画面语义同步；只在脚本文档中提供字幕文本不算完成。
- 字幕样式必须为画面上的纯文字叠加，不得使用字幕背景条、色块、气泡、卡片或半透明底；为保证可读性，只允许使用文字描边、轻微阴影或轻微发光。
- 禁止用多张静态商品图加转场、推拉或平移冒充动态视频。除非用户明确要求图片轮播视频，每条视频必须包含真实可见的商品、人物或使用动作。
- 音乐、口播和音效必须可商用或由生成工具原创，不得使用未授权音乐、影视音频、平台热门歌曲或受保护声音素材。
- 如果当前工具不能生成或合成音频与字幕，只能把结果标记为“无声视觉草稿”或“未完成”，不得交付为最终商品视频。

### Temu 视频硬性规格

- 成片比例只能使用 `1:1`、`3:4` 或 `16:9`，不得使用 `9:16`。
- 时长不得超过 `60 秒`。
- 文件大小不得超过 `100 MB`。
- 视频必须清楚包含商品主体，并突出 1-3 个基于已确认事实的核心卖点。
- 禁止 PPT、静态图片幻灯片、套图轮播或只靠转场、推拉、平移、缩放形成的伪动态视频。
- 画面必须完整铺满所选比例，不得出现上下或左右黑边。
- 不得包含平台水印、生成工具水印、品牌外水印或其他覆盖性标记。
- 任一硬性规格不符合时，Temu 视频必须判定为“未完成”。
