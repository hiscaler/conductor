# Conductor Project Instructions

执行跨境电商任务时，按本文件加载规则、检查输入、完成研究与生产，并输出合规检查和下一步动作。

## 1. 规则加载

当用户请求跨境电商、商品上架、Temu/Amazon/Shopify/Etsy/TikTok Shop 等平台内容、竞品分析、标题描述关键词、套图、AI 商品图或视频脚本时，必须按本项目规则执行，并先读取：

- `agents/cross-border-commerce-agent.md`
- `agents/subagents.md`
- `templates/chat-output.md`
- `templates/production-output.md`
- `platforms/platform-profiles.md`
- `platforms/commerce-semantic-creative-rules.md`
- `platforms/market-data-sources.md`
- `platforms/image-set-rules.md`
- `platforms/image-technical-specs.md`
- `workflows/product-to-listing.md`
- `workflows/start-guide.md`
- `workflows/creative-direction-selection.md`
- `workflows/action-menu.md`
- `workflows/output-structure.md`

当用户只有方向、品类、人群、关键词，或要求“找品”“趋势筛选”“从视频里分析商品”“参考 YouTube/TikTok/Instagram 内容找机会”时，还必须读取：

- `agents/trend-and-video-discovery-agent.md`

如涉及图片或视频生成，还必须读取：

- `agents/visual-production-agent.md`
- `platforms/video-set-rules.md`
- `platforms/video-technical-specs.md`
- `platforms/video-scene-presets.md`

## 2. 输出要求

- 正式执行完成后，聊天默认按 `templates/chat-output.md` 只展示本轮状态、相关产出、缺失与风险、已保存文件和下一步动作，不展开无关章节。
- 全部启动菜单和自然语言单项任务的下一步动作都必须受本轮明确请求约束，只能补齐、修改、重生成或查看本轮范围内的产物。完整报告里未执行或待确认的供应链、利润、合规、测试、竞品、视频、上架包和 QA 不得自动成为下一步动作。菜单 2“文案 + AI 商品图”只是示例：任一菜单都不得推荐其他菜单范围的动作；动作不足 2 个时可以只展示 1 个，不得凑数。交付前必须使用 `node scripts/next-action-scope.mjs <启动菜单编号> <动作ID...>` 校验。
- 同时必须按 `templates/production-output.md` 生成完整生产报告，文件名固定为 `上架/完整生产报告.md`。首次生产保存在 `output/{平台}-{市场}/{Listing标识}/`；再次生产时按目录版本规则改用 `output/{平台}-{市场}/{Listing标识}-v2/`、`-v3/`，不得给报告或其他文件名添加版本后缀，也不得静默覆盖。完整报告必须保留 16 个章节和所有表格；缺失信息写“未提供”或“待确认”，未执行章节写“本轮未执行”，下一步执行清单不能为空。
- 完整生产报告必须在 16 个编号章节之前生成 `问题速览`：只汇总本轮未解决的 `🔴【阻塞】`、`🟠【高风险】`、`🟡【待确认】`，显示分类数量，并使用显式稳定锚点从速览跳转到对应章节详情。问题 ID 使用 `issue-{两位章节号}-{两位问题序号}`，详情提供返回 `#issue-summary` 的链接；不得依赖中文标题自动锚点，也不得把无关章节的“本轮未执行”计为问题。
- 保存完整生产报告或独立验收报告后，运行 `node scripts/check-report-anchors.mjs <报告路径>`；锚点重复、速览链接没有对应详情或缺少 `issue-summary` 时不得交付。
- 用户选择“从选品到上架走完整流程”、要求完整上架包、执行上架前 QA/完整验收，或明确要求“完整报告”“完整 16 章”“按完整模板输出”时，聊天直接展开 `templates/production-output.md` 的 16 个章节。
- 单项文案、图片、视频、找品、竞品、供应链、利润或合规任务不得在聊天中堆叠无关的“本轮未执行”；但保存的完整报告仍须保留这些章节。
- 聊天简版缺少完成状态、相关产出、风险、保存路径或下一步动作，或者完整报告缺少 `## 1. 输入摘要` 至 `## 16. 下一步执行清单`，均视为未完成。
- 启动向导例外：用户只输入“hello”“你好”“开始”“菜单”“帮助”“使用说明”“我要用指挥家”“我该怎么用”“可以做什么”等入口短句时，按 `workflows/start-guide.md` 输出纯数字流程菜单，不输出 16 章节模板。
- 用户在启动向导或下一步菜单后输入数字时，按最近一次菜单解释数字；不得要求用户输入字母动作 ID。
- 给卖家看的章节、字段、策略、风险、报告、动作统一使用中文；只有可直接复制到目标平台、买家可见或用于搜索/广告投放的最终文案值使用目标市场语言。
- 生成标题前必须按 `platforms/market-data-sources.md` 建立标题关键词数据链。优先级为自身商品事实 > 目标平台数据 > 目标市场 Google Search 或本地搜索 > Google Trends 或本地趋势工具 > Instagram/TikTok/YouTube 等内容平台 > 其他电商平台。每个进入最终标题的主要关键词都要记录来源、抓取日期、可见信号、商品事实状态和标题位置；未获取的数据源必须如实标记。
- Temu 商品描述必须按段落生成，Markdown 中使用空行分段；每段最多 `500` 个字符，字母、数字、空格和标点符号均计入，建议每段 `350-450` 个字符。保存文案后必须运行 `node scripts/temu-description-limit.mjs <文案 Markdown 路径>`，任一段超限时先按完整句子边界拆分并重新校验，不得交付未通过文案。
- 不得编造销量、排名、评论、趋势增长率、供应链事实、认证、材质、尺寸、功能、联系方式、商品链接或授权素材。无法获取写“未获取”“受访问限制”“待确认”或“未公开”。

## 3. 动态输入与自动参考示例

- 启动菜单仅用于选择任务。用户选择后，复用当前会话信息，只收集本任务缺失的最低必要输入。
- 找品/趋势任务不要求商品名称和图片；文案任务至少需要商品名称或可匹配 SKU、目标平台、目标市场；图片/视频/完整上架资产任务还需要图库中的主白底图、用户上传商品图、可靠商品链接或足以锁定外观的资料之一；竞品分析至少需要竞品链接、榜单入口、关键词或截图之一；合规/QA 只收集待检查内容。
- 需要具体商品的任务，必须先给出带真实示例值、可直接复制修改的最小输入模板。不得只列空字段，也不得要求用户一次性填写完整产品档案。
- 请求最低必要输入时，内容生产任务必须在可复制区域外预告“商品示例回传后先生成具体创意方向，需要时输入数字选择，再正式生成产物”；不得写成“确认商品资料后直接输出标题、图片或视频”。
- 收到最低必要输入后，自动做商品识别、目标市场本地语言关键词、同类商品研究、热销参考链接、评论痛点和未来节日筛选；不要再要求用户填写核心功能、卖点、购买动机、使用场景、目标人群或定制示例。
- 自动参考示例必须包含：核心功能、希望突出卖点、主要购买动机、定制示例、目标平台热销参考链接 2-3 个、销售单位/组合形态、包含内容、销售数量、是否同款/混色/混款，以及目标平台和类目必需属性。
- 可复制代码块只放商品资料本体；“下面是自动生成的商品资料示例”“请复制整段”“如果不满意”等操作说明必须放在代码块外。
- 用户未回传商品资料示例前，不进入正式文案、图片或视频生产；回传后以用户版本作为当前产品档案。
- 正式产物统一保存到 `output/{平台}-{市场}/{Listing版本目录}/`。基础 Listing 标识的生成不变：单 SKU 且数量为 1 时使用商品主表标准 SKU；数量大于 1 或包含多个 SKU 时使用包含标准 SKU 和数量的稳定组合标识；无 SKU 商品使用 `未建档-{简短商品名}`。商品名称和类目只写入商品资料，不作为目录层级；目录末尾的 `-v2`、`-v3` 只表示生产版本，不属于 SKU 或 Listing 标识。
- 每个正式生产批次开始时，只调用一次 `node scripts/output-versioning.mjs output/{平台}-{市场}/{Listing标识}` 分配 Listing 版本目录。首次生产使用基础目录；只要基础目录或历史版本目录已存在，本批次全部产物统一写入脚本返回的下一个 `{Listing标识}-vN/` 目录。同一批次后续生成图片、补图、视频、QA 或保存文案时必须复用已分配目录，不得再次调用脚本抬升版本；用户重新发起生成、重新生成同一 SKU 或开始新的正式生产批次时才重新分配。目录内部继续使用规范中的固定文件名，禁止给文案、图片、视频、脚本、验收报告或完整报告文件名添加 `-vN`。同一未完成批次的断点续做可以新增尚不存在的暂停图片并更新该批次脚本和报告，但不得覆盖已有图片；其他情况下只有用户明确要求替换指定文件时才可跳过版本分配，并必须在写入前说明将被替换的准确路径。
- Listing 版本目录及其子目录必须按本轮实际产物按需创建。未执行供应链、利润、合规、测试、竞品、图片或视频任务时，不得创建对应空目录；完整生产报告中的“本轮未执行”只写在报告内，不能用空文件夹占位。正式生产结束前必须运行 `node scripts/check-output-layout.mjs`，发现空目录时先清理再交付。
- 事实分级内部执行：已确认事实才能进入最终买家文案；竞品高频但自身未证实的功能只作为“待确认/待验证机会”。美国市场长度默认保留 `cm` 和 `inch`，容量按品类保留 `oz` 和 `ml`。
- 节日建议必须属于目标市场、晚于当前日期，并扣除生产、定制、物流和营销准备时间；过期、来不及或关联牵强的节日不得推荐。

## 3A. 商品资料库与白底图库

- 读取 `data/product-catalog.csv` 和 `data/product-attributes.csv`：主表提供跨品类通用数据，扩展属性表提供品类参数；按 `data/products/{SPU}/{SKU}/` 加载原始商品图片。
- CSV 是商品资料库的唯一事实源。Agent 必须按固定表头读取，不得自行增删字段或改变结构。
- 每次读取 CSV 时必须先校验编码、表头、列数、必填值、SKU 唯一性、数字、日期、允许值、扩展属性 SKU 外键和本地资料路径。错误必须指出文件、行号、字段、原值和修正方式；不得静默修正可能改变事实的数据。
- 扩展属性按唯一 SKU 关联商品主表；扩展属性中的 SKU 必须来自商品主表，不得接受孤立或不存在的 SKU。
- 将 CSV 内容作为卖家提供的商品资料使用；涉及功能、性能、认证或合规的高风险声明仍须核对证据。
- 匹配优先级：当前对话中用户明确修改 > `product-catalog.csv` 的标准化 SKU 精确记录 > CSV 中的 SPU/商品名称唯一匹配 > 已确认供应商资料 > 商品图片中可直接观察的外观、数量和结构事实 > 市场建议。
- 商品名称匹配到多个 SKU 时必须让用户选择，不得擅自选择颜色、尺寸或款式。
- 图片目录由 SPU 和 SKU 推导，不在 CSV 中逐张维护路径。每个 SKU 至少需要一个无编号的 `main` 主白底图；其他可选文件为 `back`、`left`、`right`、`top`、`detail-01` 等。
- 已确认认证写入 `product-attributes.csv`，并记录来源和核验日期；未确认或禁止使用的声明写入 `product-catalog.csv` 的“风险/禁用声明”。没有证据不得创建认证属性。
- 图片支持 `png`、`jpg`、`jpeg`、`webp`。同一图片角色存在多个扩展名时必须标记冲突，不得静默任选。
- 不得借用同一 SPU 下其他 SKU 的图片。图片、视频或完整上架任务缺少 `main` 时必须提示补图；文案任务可以继续，但要标记“未加载主白底图”。
- CSV 的空值不得被竞品属性自动覆盖。当前用户明确修改可以覆盖本轮 CSV 值，但是否回写商品资料库需要用户另行授权。
- 商品“具备定制能力”和“本 Listing 是否启用卖家定制服务”必须分开记录。商品资料库中的定制内容、位置和工艺只证明能力，不自动触发定制生产。
- 正式生产前必须确定卖家定制服务：`不提供`、`必选`、`可选` 或 `待确认`。只有 `必选` 和 `可选` 才进入定制母版、最终定制效果和定制操作示意流程；`不提供` 时禁止生成卖家定制操作图。
- 卖家定制服务为 `待确认` 且现有上下文无法判断时，只询问一个纯数字问题：`1. 不提供定制`、`2. 必须定制`、`3. 定制可选`。不得把普通成品和 DIY 空白基底拆成同级选项。
- 选择 `不提供` 后，再根据已确认事实记录非卖家定制定位：`普通成品` 或 `DIY 空白基底`。只有用户、供应商资料或商品事实明确支持 DIY/印刷/雕刻等用途时才能标记为 `DIY 空白基底`；仅因表面空白不得推断。两种定位都不得出现 `Add Your Text`、`Upload Your Image` 等卖家定制入口。
- 原始图库不得被生成资产覆盖；生成结果仍保存到 `output/`。

## 3B. 商品标识解析

- 接收用户直接输入的商品名称、单个 SKU 或以逗号分隔的多个 SKU；字段标签可省略。
- SKU 匹配前去除整个输入及每个组合项首尾空格，英文字母不区分大小写；保留 SKU 内部空格、横线、下划线和其他字符。匹配成功后使用商品主表中的标准 SKU。
- 商品主表中两个 SKU 去除首尾空格并忽略大小写后相同时，立即标记数据冲突并停止匹配，要求修正 CSV。
- 单项输入先做 SKU 精确匹配；未命中时再做商品名称精确匹配。名称唯一命中时读取该商品；名称命中多个 SKU 时列出候选并要求选择。
- SKU 和商品名称均未精确命中时，搜索 CSV 中的相似商品。相似商品只用于识别类目、建议待填属性和生成参考，不得继承其尺寸、材质、功能、认证、包装或其他商品事实。没有可靠相似项时，将输入作为新商品处理。
- `*` 或 `×` 仅在左侧为商品标识、右侧为正整数时解析为数量；未写数量时默认 1。数量无效时要求用户修正，不得猜测。
- 一次输入多个商品标识时，按一个组合 listing 处理；相同标准 SKU 合并并累加数量。
- 组合中未匹配的项目按新商品处理；先复用已匹配 SKU 的资料，只针对未匹配项目收集当前任务缺失的最低必要信息。
- 完成匹配后自动合并主表、扩展属性和图库资料，并据此生成可复制商品示例；不得重复询问已经读取的信息。

## 3C. 创意方向选择

- 用户回传商品资料示例后，正式生成文案、图片或视频前，必须按 `workflows/creative-direction-selection.md` 判断是否需要创意方向选择。
- 触发创意方向选择时，先输出 3-5 个创意方向的纯数字菜单，不输出聊天结果或 16 章完整报告。
- 用户只需回复一个创意编号；输入 `0` 表示系统自动选择。为兼容旧对话仍接受“自动选择”，但界面提示统一使用数字。选择后不得再次要求用户补写创意字段，必须生成可执行创意方案并继续执行原任务。
- 创意方案必须能落到标题、描述、关键词、图片脚本和视频脚本；不能只输出抽象概念。
- 不展示菜单时仍须形成创意策略：用户已给方向则整理并沿用；用户要求直接输出或商品是强规格标准品则生成事实优先的默认策略；只有非内容生产任务才写“不适用”。
- 创意只能组织已确认事实和市场建议，不得把未确认功能、材质、认证、授权、销量、尺寸或效果写成商品事实。
- 创意方案只覆盖本轮实际请求的内容载体；未请求的文案、图片或视频字段写“本轮未执行”。

## 4. 销售单位与组合形态

- 根据本轮 `SKU × 数量` 列表计算销售单位和组合形态。
- 必须记录组合形态、实际包含内容、总数量、款式关系和单件折算价，并归一化不同语言及格式的数量表达。
- 同一 SKU 多件、同系列不同变体和不同组件 SKU 应分别识别为对应的多件、混合或套装关系；存在歧义时先标记待确认。
- 定制类组合商品还必须记录“组合内定制关系”：同一内容应用全部、各组件独立定制、按组件分别定义、仅部分组件可定制或待确认。未确认时不得在文案或图片中暗示某一种关系。
- 没有明确数量或组合信息时默认单件，不得擅自推断。
- 文案、图片、包装清单、价格和平台属性必须与本轮实际售卖内容一致。

## 5. 找品、趋势与市场数据

- 用户要求趋势或短视频场景找品时执行本节；用户已提供具体商品并要求直接产出时跳过本节。
- 只有想法或方向时，先判断目标国家/地区数据源、本地语言关键词、趋势信号、短视频真实使用场景、可销售商品、品牌真空、供应链、小批量测试和平台适配。
- 候选商品先经过趋势、场景、品牌真空、供应链和风险评分，再进入标题、描述、套图和视频脚本产出。
- 非美国市场不得只用英文关键词或美国平台数据做结论；必须先生成本地语言关键词，并选择目标市场常用的搜索/趋势平台、内容平台和电商验证平台。
- 目标平台热销参考链接默认提供 2-3 个优质链接；无法访问或目标平台待定时写“未获取”，或提供相近平台参考并明确不能替代目标平台结论。

## 6. 供应链、链接与登录

- 完整流程必须包含供应链候选供应商信息。优先查询 1688、Alibaba、义乌购或其他可访问供应链平台，默认输出 3-5 家候选。
- 中国采购、国内工厂、1688、义乌货源、跨境供货或中国供应链场景，默认来源顺序为：`1688` → `义乌购等国内专业货源平台` → `中国制造网等可验证中国供应商来源` → `Alibaba 国际站补充`；必须先用中文关键词。
- 供应商候选应记录平台、店铺名称、商品/店铺网址、主营产品、店铺年限、销量/成交公开口径、评分、MOQ、价格区间、交期、定制能力、小批量能力、公开联系方式、数据来源、抓取日期、访问限制和初步建议。
- 联系方式只采集公开展示的企业或店铺联系方式；未公开、需登录、验证码限制或无法访问时写“未公开”“需登录”或“未获取”。
- 有效供应链报价必须同时包含平台、店铺名称、商品/店铺网址、价格区间、MOQ 或起订条件、抓取日期和获取状态。泛化价格区间、没有店铺名称或商品链接的内容只能写入“无效参考/待补充”，不得作为采购价结论。
- 已获得有效公开价格区间后，`目标售价/成本区间` 和利润测算不得继续只写“待确认”；必须形成采购价预估区间，并列出仍需供应商确认的样品、阶梯价、运费、包装、定制等成本项。
- 交付给用户的供应商商品链接或店铺链接，必须实际打开验证最终地址和页面类型；区分商品详情页、供应商店铺页、搜索结果页、平台首页、登录页、验证码/风控页、下架页和错误页。不得编造平台商品 ID、店铺地址或 URL。
- 遇到登录、验证码或账号权限时，只请求用户完成验证；验证后继续执行关键词生成、搜索、筛选、页面打开和数据提取。只有工具无法继续或持续触发风控时，才请求用户提供应急协助。

## 7. 图片与定制商品

- “套图”“输出套图”“生成套图”“生成一套商品图”默认表示生成 AI 图片，不只是输出脚本；只有用户明确说不生成图片时才改为脚本模式。
- 每张商品图必须是独立文件；禁止用一张拼图、九宫格、预览图或带多个缩略图的单张图片代替整套图。
- 套图结构、数量、文案、尺寸单位和平台规格严格以 `platforms/image-set-rules.md` 与 `platforms/image-technical-specs.md` 为准，未达到完成标准必须明确列出缺失项。
- 定制商品必须先形成最终定制效果，再生成主图、包装图、场景图和说明图；占位文字不得冒充最终定制内容。
- 同一 Listing 的一套定制商品图必须先生成并验收主图，再把主图中的最终定制效果登记为本套唯一“定制母版”。其余图片必须以已验收主图作为商品与定制内容的参考锚点，保持文字内容与拼写、字体风格、图案、颜色、布局、定制位置和相对比例一致，只允许改变视角、透视、遮挡、背景、道具和信息标注。不得逐张独立随机生成新的定制方案。
- 只有专门的“定制选项/效果示例图”或真实多 SKU/多变体对比图可以在同一张图内展示多个定制方案；必须明确分区和标注，且这些备选方案不得随机替换其他套图中的定制母版。无受控例外时，任一图片的定制内容漂移都必须判定该图不可用，并将整套图状态保持为“未完成”。
- 组合商品的定制操作示意必须先按商品类型归组。结构、定制位置、输入类型和定制工艺相同的组件属于同一类型，即使 SKU、颜色或数量不同，也只选择其中一个代表商品演示一次；其他同类型组件仅用于展示套装数量和款式，不重复箭头或标签，也不得把不同输入类型错误分配给不同组件。不同类型或定制机制不同的组件才分别说明。
- 定制操作示意图、步骤图和卖点图必须先依据商品事实、实际输入字段、素材类型和画面用途选择标签，再按输入类型、指向区域和操作目的做语义去重。姓名定制、自由文字、照片和广义图片应分别按上下文选择 `Name`、`Text`、`Photo` 或 `Image`，不得固定套用某一组示例词；同一操作只保留一个标签，同义表达不得并列。发现同义标签重复、标签与实际输入不符或同图句式混乱时，相关单图必须判定为不可用并重生成。
- 生成素材默认排除未经授权的品牌、人物、作品及其他受保护内容。
- 所有画面声明必须基于已确认事实，不得编造功能、性能、认证或适用性。
- 图片生产前执行资料充足性门禁：商品身份/外观、SKU/变体、实际售卖数量或核心组件等决定整套真实性的事实不足时，暂停整套图片；尺寸、包装形式、材质细节、认证或特定功能等只影响单张图片的事实不足时，仅暂停对应图片，其他不受影响图片可以继续生成，并将整套状态记为“未完成”；与当前画面无关的缺失信息直接省略，不得阻塞生产。
- `待确认`、`未确认`、`未提供`、`假设`、`内部参考`、`资料不足` 及其目标市场语言翻译只记录在商品资料、完整生产报告、套图脚本或图片验收报告中，不得作为买家可见文字、徽章或图标进入商品图。未确认包装形式时可以只展示已确认的到手商品，不得生成具体包装；实际到手内容不明确时必须暂停相关图片，不得用免责声明替代事实。
- 暂停图片必须记录图片序号、图片类型、暂停原因、最低补充资料、可直接回复的示例和恢复目标 Listing 版本目录。用户直接补充所需事实或选择“继续生成缺失图片”后，只生成状态为“暂停/未生成”的图片，并更新该版本的套图脚本、图片验收报告和完整生产报告，不重复生成已完成图片。
- 断点续做仅适用于同一未完成生产批次：目标图片文件尚不存在时继续写入原 Listing 版本目录，不重新分配 `-vN`；商品身份、外观、SKU/变体、销售数量或核心组件发生变化时视为新生产批次，必须分配新版本并重新检查全部受影响图片；目标文件已经存在但不可用时按重生成处理，默认创建新版本，只有用户明确授权替换准确路径时才覆盖。
- 尺寸/规格图始终保留真实尺寸、双单位和对应箭头；只有商品大小难以直观判断、其他场景图未提供可靠尺度感且存在稳定参照时，才增加尺寸参考物。
- 尺寸参考物必须按真实比例呈现，并标注 `Size Reference`；不属于包装内容时同时标注 `Not Included`。不得用参考物替代精确尺寸，不得把手掌、人体、型号不明的手机、地区规格不一的硬币或饮料容器作为精确尺度。
- 对装、多件装和同类型组合的尺寸图优先标注 `Per Item` 或 `Dimensions Shown Per Item`，并同时说明套装数量；不得用重复展示全部商品或增加参考物代替单件尺寸与整套数量关系。

## 8. 视频

- 最终商品视频必须同时包含可听音频和烧录字幕；缺少任一项，整条视频判定为“未完成”。
- 音频、字幕、动态性、授权和平台规格严格以 `platforms/video-set-rules.md`、`platforms/video-technical-specs.md` 与 `platforms/video-scene-presets.md` 为准。
- 口播必须使用目标市场语言，与广告故事和画面匹配，具有自然情绪与节奏，不得机械朗读文本。
- 禁止用静态图片轮播或简单镜头运动冒充动态视频，除非用户明确要求该形式。
- 当前工具不能生成或合成音频与字幕时，只能标记为“无声视觉草稿”或“未完成”，不得交付为最终商品视频。
