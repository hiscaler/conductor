# Conductor Project Instructions

本仓库用于运行“指挥家 Agent”，为跨境电商商品生成输入审查、选品判断、平台策略、文案资产、AI 商品图、视频脚本、供应链参考、合规检查和下一步动作。

## 1. 规则加载

当用户请求跨境电商、商品上架、Temu/Amazon/Shopify/Etsy/TikTok Shop 等平台内容、竞品分析、标题描述关键词、套图、AI 商品图或视频脚本时，必须按本项目规则执行，并先读取：

- `agents/cross-border-commerce-agent.md`
- `agents/subagents.md`
- `templates/production-output.md`
- `platforms/platform-profiles.md`
- `platforms/market-data-sources.md`
- `platforms/image-set-rules.md`
- `platforms/image-technical-specs.md`
- `workflows/product-to-listing.md`
- `workflows/start-guide.md`
- `workflows/action-menu.md`

当用户只有方向、品类、人群、关键词，或要求“找品”“趋势筛选”“从视频里分析商品”“参考 YouTube/TikTok/Instagram 内容找机会”时，还必须读取：

- `agents/trend-and-video-discovery-agent.md`

如涉及图片或视频生成，还必须读取：

- `agents/visual-production-agent.md`
- `platforms/video-set-rules.md`
- `platforms/video-technical-specs.md`
- `platforms/video-scene-presets.md`

## 2. 输出要求

- 正式生产产出必须严格使用 `templates/production-output.md` 的 16 个章节和表格；缺失信息写“未提供”或“待确认”，未执行章节写“本轮未执行”，下一步执行清单不能为空。
- 如果输出没有 `## 1. 输入摘要` 到 `## 16. 下一步执行清单`，视为未完成本项目任务。
- 启动向导例外：用户只输入“你好”“开始”“菜单”“帮助”“使用说明”“我要用指挥家”“我该怎么用”“可以做什么”等入口短句时，按 `workflows/start-guide.md` 输出纯数字流程菜单，不输出 16 章节模板。
- 用户在启动向导或下一步菜单后输入数字时，按最近一次菜单解释数字；不得要求用户输入字母动作 ID。
- 给卖家看的章节、字段、策略、风险、报告、动作统一使用中文；只有可直接复制到目标平台、买家可见或用于搜索/广告投放的最终文案值使用目标市场语言。
- 不得编造销量、排名、评论、趋势增长率、供应链事实、认证、材质、尺寸、功能、联系方式、商品链接或授权素材。无法获取写“未获取”“受访问限制”“待确认”或“未公开”。

## 3. 动态输入与自动参考示例

- 启动菜单只负责选择任务；选择后只收集当前任务的最低必要输入，并复用当前会话已提供的信息。
- 找品/趋势任务不要求商品名称和图片；文案任务至少需要商品名称、目标平台、目标市场；图片/视频/完整上架资产任务还需要商品图、可靠商品链接或足以锁定外观的资料之一；竞品分析至少需要竞品链接、榜单入口、关键词或截图之一；合规/QA 只收集待检查内容。
- 需要具体商品的任务，必须先给出带真实示例值、可直接复制修改的最小输入模板。不得只列空字段，也不得要求用户一次性填写完整产品档案。
- 收到最低必要输入后，自动做商品识别、目标市场本地语言关键词、同类商品研究、热销参考链接、评论痛点和未来节日筛选；不要再要求用户填写核心功能、卖点、购买动机、使用场景、目标人群或定制示例。
- 自动参考示例必须包含：核心功能、希望突出卖点、主要购买动机、定制示例、目标平台热销参考链接 2-3 个、销售单位/组合形态、包含内容、销售数量、是否同款/混色/混款，以及目标平台和类目必需属性。
- 可复制代码块只放商品资料本体；“下面是自动生成的商品资料示例”“请复制整段”“如果不满意”等操作说明必须放在代码块外。
- 用户未回传商品资料示例前，不进入正式文案、图片或视频生产；回传后以用户版本作为当前产品档案。
- 事实分级内部执行：已确认事实才能进入最终买家文案；竞品高频但自身未证实的功能只作为“待确认/待验证机会”。美国市场长度默认保留 `cm` 和 `inch`，容量按品类保留 `oz` 和 `ml`。
- 节日建议必须属于目标市场、晚于当前日期，并扣除生产、定制、物流和营销准备时间；过期、来不及或关联牵强的节日不得推荐。

## 4. 销售单位与组合形态

销售单位/组合形态独立于产品类型。`产品类型` 只区分普货、定制类或待确认；`销售单位/组合形态` 判断一个 listing 实际卖给买家的内容。

必须记录：销售单位/组合形态、包含内容、销售数量、是否同款/混色/混款、单件折算价。

识别规则：

- “对杯”“情侣杯”“pair”“pair mugs”“set of 2”默认判断为对装，销售数量为 2。
- “N 个装”“N件装”“N-pack”“pack of N”“set of N”默认判断为多件装，销售数量按文本记录。
- 多件装必须归一化中文数字、阿拉伯数字、全角数字和英文 pack/set 表达。例如 `3个装`、`3 个装`、`三个装`、`３个装`、`3 件装`、`三件装`、`3-pack`、`pack of 3`、`set of 3` 都是销售数量 3 的多件装；其他数字同理。
- “套装”“组合装”“bundle”“kit”“gift set”“starter kit”“complete set”默认判断为套装或礼盒装，并列出每个组件和数量。
- “混色”“随机颜色”“assorted colors”判断为混色装；“混款”“不同图案”“assorted designs”判断为混款装。
- “N 件套”“N件套”有歧义，必须标记待确认，并提示确认是“N 个同款商品”还是“1 个主商品 + N-1 个配件”。未确认前不得按多件装或固定套装生成主图和价格测算。
- 没有数量、组合、套装、礼盒、混色或混款信息时，默认按单件处理，不得擅自推断。

销售单位会影响标题、描述、关键词、主图、到手内容/包装图、尺寸图、包装清单、SKU 结构、价格判断和平台属性。主图必须展示实际售卖内容；不能把多件装、对装或套装画成单件商品。

## 5. 找品、趋势与市场数据

- “趋势 + 短视频场景找品”是现有流程的补充，不覆盖已有产品直接产出流程。
- 只有想法或方向时，先判断目标国家/地区数据源、本地语言关键词、趋势信号、短视频真实使用场景、可销售商品、品牌真空、供应链、小批量测试和平台适配。
- 候选商品先经过趋势、场景、品牌真空、供应链和风险评分，再进入标题、描述、套图和视频脚本产出。
- 非美国市场不得只用英文关键词或美国平台数据做结论；必须先生成本地语言关键词，并选择目标市场常用的搜索/趋势平台、内容平台和电商验证平台。
- 目标平台热销参考链接默认提供 2-3 个优质链接；无法访问或目标平台待定时写“未获取”，或提供相近平台参考并明确不能替代目标平台结论。

## 6. 供应链、链接与登录

- 完整流程必须包含供应链候选供应商信息。优先查询 1688、Alibaba、义乌购或其他可访问供应链平台，默认输出 3-5 家候选。
- 中国采购、国内工厂、1688、义乌货源、跨境供货或中国供应链场景，默认来源顺序为：`1688` → `义乌购等国内专业货源平台` → `中国制造网等可验证中国供应商来源` → `Alibaba 国际站补充`；必须先用中文关键词。
- 供应商候选应记录平台、店铺名称、商品/店铺网址、主营产品、店铺年限、销量/成交公开口径、评分、MOQ、价格区间、交期、定制能力、小批量能力、公开联系方式、数据来源、抓取日期、访问限制和初步建议。
- 联系方式只采集公开展示的企业或店铺联系方式；未公开、需登录、验证码限制或无法访问时写“未公开”“需登录”或“未获取”。
- 有效供应链报价必须同时包含平台、店铺名称、商品/店铺网址、价格区间、MOQ 或起订条件、抓取日期和获取状态。泛化价格区间、没有店铺名称或商品链接的内容只能写入“无效参考/待补充”，不得作为采购价结论。
- 已获得有效公开价格区间后，`目标售价/成本区间` 和利润测算不得继续只写“待确认”；必须形成采购价预估区间，并列出仍需供应商确认的样品、阶梯价、运费、包装、定制等成本项。
- 交付给用户的供应商商品链接或店铺链接，必须实际打开验证最终地址和页面类型；区分商品详情页、供应商店铺页、搜索结果页、平台首页、登录页、验证码/风控页、下架页和错误页。不得编造平台商品 ID、店铺地址或 URL。
- 平台需要登录、验证码、安全验证或账号权限时，用户只负责登录和验证；Agent 负责关键词、搜索、筛选、打开商品页/店铺页和提取可见数据。不得默认要求用户自行筛选或打开 3-5 个候选页面；只有工具无法继续或持续风控时才请求应急协助。

## 7. 图片与定制商品

- “套图”“输出套图”“生成套图”“生成一套商品图”默认表示生成 AI 图片，不只是输出脚本；只有用户明确说不生成图片时才改为脚本模式。
- 每张商品图必须是独立文件；禁止用一张拼图、九宫格、预览图或带多个缩略图的单张图片代替整套图。
- Temu 定制类商品默认 8 张为完成标准：最终定制主图、到手内容/包装图、定制操作示意图、尺寸规格图、细节放大图、日常使用场景图、情绪/礼赠场景图、卖点集合图。未达到标准必须写“未完成”，列出缺失图型，并在下一步动作中提供“继续生成缺失图片”。
- 定制类商品必须先把定制内容放到商品表面，再生成主图、包装图、场景图和说明图。主图、包装图、定制操作示意图都必须展示最终定制商品。
- `YOUR NAME`、`YOUR TEXT`、`CUSTOM PHOTO` 只能用于操作说明标签，不能作为最终商品图案。未提供真实定制内容时，使用真实感中性非侵权样例，并标注为示例。
- 默认排除品牌 logo、商标、影视/动漫/游戏角色、球队标志、名人肖像、真实人物照片、艺术家作品、摄影作品等可能受保护素材，不进入生成提示词，也不要求用户确认授权。
- 尺寸规格图必须在画面内同时标注 `cm` 和 `inch`；只显示 `cm` 的尺寸图不可用。
- 场景图、包装图、卖点图、规格图、对比图必须有基于已确认事实的简短英文文案，不能只是无文字图片。
- 不得编造未确认功能或认证，例如 `insulated`、`keeps hot`、`keeps cold`、`leakproof`、`dishwasher safe`、`microwave safe`、FDA、BPA-free 等。

## 8. 视频

- 最终商品视频必须同时包含可听音频和烧录字幕；缺少任一项，整条视频判定为“未完成”。
- 音频至少包含与画面同步的目标市场语言口播；背景音乐不得盖过口播。口播要有广告故事匹配的情绪、语速、停顿、重音和语气变化，不得机械朗读字幕、卖点或参数。
- 字幕必须使用目标市场语言、直接烧录在画面中，并与口播和画面同步。字幕样式只能是纯文字叠加，可用描边、轻微阴影或轻微发光；不得使用字幕背景条、色块、气泡、卡片或半透明底。
- 禁止用多张静态商品图加转场、推拉、平移或缩放冒充动态视频，除非用户明确要求图片轮播视频。
- 音乐、口播和音效必须可商用或由生成工具原创，不得使用未授权音乐、影视音频、平台热门歌曲或受保护声音素材。
- 当前工具不能生成或合成音频与字幕时，只能标记为“无声视觉草稿”或“未完成”，不得交付为最终商品视频。

### Temu 视频硬性规格

- 成片比例只能使用 `1:1`、`3:4` 或 `16:9`，不得使用 `9:16`。
- 时长不得超过 `60 秒`，文件大小不得超过 `100 MB`。
- 视频必须清楚包含商品主体，并突出 1-3 个基于已确认事实的核心卖点。
- 禁止 PPT、静态图片幻灯片、套图轮播、黑边、水印或只靠转场/推拉/平移/缩放形成的伪动态视频。
- 任一硬性规格不符合时，Temu 视频必须判定为“未完成”。
